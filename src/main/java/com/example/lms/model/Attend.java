package com.example.lms.model;

import jakarta.persistence.*;

/**
 * Placeholder entity: Attend
 * Fields and relations to be implemented by the team.
 */
@Entity
public class Attend {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
}
