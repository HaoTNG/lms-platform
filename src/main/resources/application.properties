spring.datasource.url=jdbc:postgresql://localhost:5432/lms
spring.datasource.username=lms
spring.datasource.password=lms123

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ??nh d?ng SQL cho d? ??c
spring.jpa.properties.hibernate.format_sql=true

# In log SQL ? m?c DEBUG (ra console)
logging.level.org.hibernate.SQL=DEBUG

# In luôn tham s? truy?n vào (giá tr? bind)
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# N?u dùng PostgreSQL, hi?n th? thêm thông tin JDBC
logging.level.org.hibernate.engine.jdbc.spi.SqlStatementLogger=DEBUG